---
import Header from "../components/Header.astro";
import Container from "../components/elements/Container.astro";
type Props = {
	title: string;
	description: string;
};

const { title, description } = Astro.props;
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta
			name="viewport"
			content="width=device-width, initial-scale=1.0"
		/>
		<link
			rel="icon"
			type="image/x-icon"
			href="/saris-favicon.png"
		/>
		<title>{title}</title>
		<meta
			name="description"
			content={description}
		/>
	</head>
	<body class="text-slate-500 bg-slate-50">
		<Header />
		<main class="space-y-8 sm:space-y-16 py-8 sm:py-16">
			<slot />
		</main>
	</body>
</html>
